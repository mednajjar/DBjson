[build]
  publish = "public"

[[redirects]]
  from = "/"
  to = "/db.json"
  status = 302

[[headers]]
  # Define which paths this specific [[headers]] block will cover.
  for = "/db.json"
    [headers.values]
    Access-Control-Allow-Origin = "*"